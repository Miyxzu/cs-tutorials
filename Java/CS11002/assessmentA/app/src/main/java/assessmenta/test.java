package assessmentA;

import java.util.*;

public class test {
    void main(String[] args) {
        Scanner in = new Scanner(System.in);
        // String[] arr = {"Helios", "Heliocentric", "ppx", "omE", "Helium", "Flying", "xdding", "xddWalking", "chovying"};
        // String check = "";
        // System.out.print("Enter starting name of Species (i.e. Heli*):\n>> ");
        // String search = in.nextLine();
        // for (int i = 0; i < search.length(); i++) {
        //     if (search.substring(i).contains("*")) {
        //         if (search.indexOf("*") == 0) {
        //             check = search.substring(1, search.length());
        //             break;
        //         }
        //         check = search.substring(0, (search.indexOf("*")));
        //         break;
        //     }
        // }

        // System.out.println(check);

        // for (int i = 0; i < arr.length; i++) {
        //     if (arr[i].startsWith(check)) {
        //         System.out.println(arr[i]);
        //     }
        //     if (arr[i].endsWith(check)) {
        //         System.out.println(arr[i]);
        //     }
        // }

                        System.out.print("Search by:\nName\nRegion\nName/Region\n>>");


        in.close();
    }
}
